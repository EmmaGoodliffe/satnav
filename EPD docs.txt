EPD
---

__init__
	(hard-coded constants)
digital_write/digit_read
	write/read certain pin
delay_ms
	sleep
spi_writebyte
	write binary data to SPI bus
module_exit
	permanently trigger reset pin
reset
	briefly trigger reset pin and then revert
send_command
	set data/command pin to command
	select chip
	send binary command along SPI bus
send_data/send_data1
	set data/command pin to command
	select chip
	send binary data along SPI bus
ReadBusy
	return once display is not busy
TurnOnDisplay
	send display update command
	send data
	send activation command
lut
	send LUT (look-up table?) command
SetLut
	override default source voltage etc.
SetWindow
	override window RAM locations?
SetCursor
	override RAM current position?
init/init_4Gray
	return overrides to defaults
display/display_Base/display_4Gray/display_Partial
	wrapper for send_data1
Clear
	set to all one colour
sleep
	hardware sleep mode

demo
----

Clear
fill (parent):	prep background
text (parent):	prep text
display:	update
line (parent):	prep line
rect (parent):	prep rectangle
fill_rect (parent): prep filled rect
display_Base:	update before partial updates
display_Partial: partially update